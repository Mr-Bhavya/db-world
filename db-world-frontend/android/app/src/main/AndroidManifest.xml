<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBarLaunch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links — a shared https://db-world.in/db-world/… link opens
                 straight in the app instead of the browser. autoVerify makes Android
                 fetch /.well-known/assetlinks.json at install time, so there is no
                 "open with" chooser. That file is served inline by nginx (10-app.conf
                 in the db-world-config repo) and must list this package + the release
                 keystore's SHA-256, or verification fails and links open in Chrome.
                 Scoped to /db-world so only real app routes are claimed.
                 The URL is delivered to the web layer via Capacitor's appUrlOpen and
                 routed by src/shared/deeplink/appLinks.js. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="db-world.in" />
                <data android:scheme="https" android:host="www.db-world.in" />
                <data android:pathPrefix="/db-world" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.db.dbworld.player.probe.HdrProbeActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBarLaunch" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Keeps the process alive while downloads are running so the OS does not
             kill them when the app is backgrounded. -->
        <service
            android:name="com.db.dbworld.download.DownloadForegroundService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="dataSync" />
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- In-app updater: install the downloaded release APK (sideloaded distribution) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <!-- Required for DownloadManager to save to public Movies folder on Android ≤ 9 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Write downloads straight into the public Download/DB-World folder on Android 11+ (granted once). -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <!-- Download progress notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <!-- Foreground service keeps downloads running when the app is backgrounded -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Network-type awareness for Wi-Fi-only / auto-resume on reconnect -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Keep the CPU + Wi-Fi radio awake while downloading so transfers don't freeze under
         screen-off / Doze / battery-saver (the download-stops-on-its-own bug). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
