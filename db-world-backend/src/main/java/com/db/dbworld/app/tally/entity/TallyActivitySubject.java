package com.db.dbworld.app.tally.entity;

/** What kind of row an activity entry is about, paired with its id. */
public enum TallyActivitySubject {
    GROUP,
    MEMBER,
    EXPENSE,
    SETTLEMENT
}
